{
  "name": "guideme-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Universal Tutorial Engine — Browser Extension and Headless Engine Monorepo",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run --prefix apps/chrome-extension dev",
    "build": "npm run --prefix apps/chrome-extension build",
    "test": "node --test tests/**/*.test.js",
    "clean": "rm -rf node_modules apps/*/node_modules packages/*/node_modules apps/*/.output apps/*/.wxt"
  },
  "keywords": [
    "tutorial",
    "onboarding",
    "chrome-extension",
    "wxt",
    "react",
    "walkthrough"
  ],
  "author": "",
  "license": "MIT"
}
